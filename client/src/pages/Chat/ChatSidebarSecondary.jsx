import PropTypes from "prop-types";

const SecondarySidebar = ({ className }) => {
  return <div className={className}>SecondarySidebar</div>;
};

SecondarySidebar.propTypes = {
  className: PropTypes.string,
};

export default SecondarySidebar;
